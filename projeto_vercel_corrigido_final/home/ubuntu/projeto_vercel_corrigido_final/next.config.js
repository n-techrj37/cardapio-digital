/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Outras configurações podem ser adicionadas aqui se necessário no futuro
  // Por exemplo, para otimização de imagens ou variáveis de ambiente.
  // Para o deploy, um arquivo básico é geralmente suficiente se não houver requisitos específicos.
};

module.exports = nextConfig;

